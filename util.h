#pragma once
void toHex(char ch, char* c1, char* c2);
void readFromTxt(const char path[]);
void toCharArrayHex(const char* arr, int size, char* hexArray);
void printHexToBinary(char* hexArray, int length);
void printHex(char hex);